import type { ReactNode } from 'react';

export const metadata = { title: 'Labor-AI Research Tools' };

export default function ResearchLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
